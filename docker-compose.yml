services:
  backend:
    build: ./backend
    restart: unless-stopped
    expose:
      - "3000"
    volumes:
      - backend_data:/app/data

volumes:
  backend_data:

  frontend:
    build: ./frontend
    restart: unless-stopped
    ports:
      - "80:80"
    depends_on:
      - backend
