services:
  backend:
    build: ./backend
    restart: unless-stopped
    expose:
      - "3000"
    volumes:
      - backend_data:/app/data

  frontend:
    build: ./frontend
    restart: unless-stopped
    ports:
      - "80:80"
    depends_on:
      - backend

volumes:
  backend_data:
